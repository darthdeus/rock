// this is a function
fn foo(x: string, y, z: num) -> num {
  print(z, x + y);

  return 5;
}

foo(5);

fn bar(hel,
lo:string)
-> string {
    let f= hel + lo;
    print(f[4-5].rock);
    return "hel";
    return "lul" + hel + "sad"+lo;
}
